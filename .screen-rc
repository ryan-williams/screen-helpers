
export ssh="$(dirname "${BASH_SOURCE[0]}")"

alias sls="screen -ls"
alias sld=screen-list-detached

alias sr="screen -r"
alias srs="screen -r -S"

alias smd="screen -m -d"
alias smds="screen -m -d -S"

alias srds="screen -D -S"

alias shc=screen-hardcopy
alias shca=screen-hardcopy-all

alias sqa=screen-quit-all

alias scs=screens

alias ss="screen -S"
alias sa=screen-attach

alias slg=screen-log
alias sle=screen-log-env

defn sled screen-log-env-detach

export SCREEN_LOG_DIR="$HOME/sld"

defn sck screen-quit
